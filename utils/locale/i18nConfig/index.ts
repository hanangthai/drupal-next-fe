const i18nConfig = {
  locales: ['en', 'zh-hans'],
  defaultLocale: 'en',
  localeDetector: false as unknown as false,
  serverSetCookie: 'never' as 'never' | 'if-empty' | 'always',
};

export default i18nConfig;
